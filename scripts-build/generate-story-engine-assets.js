import fs from 'node:fs/promises';import path from 'node:path';
const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),'..');const out=path.join(root,'assets/story-engine');const files=await fs.readdir(out).catch(()=>[]);const styles=files.filter(f=>/^styles-.*\.css$/.test(f)||f==='styles.css').map(f=>`/assets/story-engine/${f}`);const scripts=files.filter(f=>/^(polyfills|main)-.*\.js$/.test(f)||/^(polyfills|main)\.js$/.test(f)).sort((a,b)=>a.startsWith('polyfills')?-1:b.startsWith('polyfills')?1:0).map(f=>`/assets/story-engine/${f}`);await fs.writeFile(path.join(out,'story-engine-assets.json'),JSON.stringify({version:'1.0.0',generatedAt:new Date().toISOString(),styles,scripts},null,2)+'\n');console.log(`[OK] story-engine-assets.json: ${styles.length} CSS, ${scripts.length} JS`);
