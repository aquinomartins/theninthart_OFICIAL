import {bootstrapApplication} from '@angular/platform-browser';import {provideHttpClient} from '@angular/common/http';import {provideZonelessChangeDetection} from '@angular/core';import {StoryEngineComponent} from './app/story-engine.component';
const host=document.querySelector('tna-story-engine');if(host){bootstrapApplication(StoryEngineComponent,{providers:[provideZonelessChangeDetection(),provideHttpClient()]}).catch(error=>{console.error('[TNAStoryEngine] bootstrap',error);window.dispatchEvent(new CustomEvent('tna:story-engine-error',{detail:{source:'angular',code:'BOOTSTRAP_FAILED',phase:'bootstrap',recoverable:false,timestamp:Date.now()}}))})}
