import {ChangeDetectionStrategy,Component,ElementRef,Input,ViewChild,signal} from '@angular/core';import {Quadrant,QuadrantSlot} from '../../story-engine.models';import {StoryPanelComponent} from '../story-panel/story-panel.component';
type StoryGroupId='assembly'|'title'|'urgency'|'rupture'|'establish'|'explore'|'threat'|'climax'|'return'|'kitchen'|'consequences';
interface StoryGroup{id:StoryGroupId;label:string;quadrantIds:readonly string[]}
@Component({selector:'tna-story-grid',standalone:true,imports:[StoryPanelComponent],templateUrl:'./story-grid.component.html',styleUrl:'./story-grid.component.css',changeDetection:ChangeDetectionStrategy.OnPush})export class StoryGridComponent{@Input({required:true})quadrants!:readonly Quadrant[];@Input({required:true})slotForQuadrant!:(id:string)=>QuadrantSlot|undefined;@ViewChild('carouselViewport')private carouselViewport?:ElementRef<HTMLElement>;readonly atCarouselStart=signal(true);readonly atCarouselEnd=signal(false);readonly groups:readonly StoryGroup[]=[{id:'assembly',label:'Montagem',quadrantIds:['q01','q02','q03','q04','q05','q06']},{id:'title',label:'Título',quadrantIds:['q07']},{id:'urgency',label:'Urgência',quadrantIds:['q08','q09','q10']},{id:'rupture',label:'Ruptura diagonal',quadrantIds:['q11','q12']},{id:'establish',label:'Estabelecimento',quadrantIds:['q13']},{id:'explore',label:'Exploração',quadrantIds:['q14','q15','q16']},{id:'threat',label:'Ameaça',quadrantIds:['q17','q18']},{id:'climax',label:'Clímax',quadrantIds:['q19']},{id:'return',label:'Explosão e retorno',quadrantIds:['q20']},{id:'kitchen',label:'Cozinha',quadrantIds:['q21','q22','q23']},{id:'consequences',label:'Consequências',quadrantIds:['q24','q25','q26','q27','q28','q29']}];readonly mainGroups=this.groups.filter(g=>g.id!=='consequences');readonly carouselGroup=this.groups.find(g=>g.id==='consequences')!;quadrantById(id:string){return this.quadrants.find(q=>q.id===id)}scrollCarousel(direction:-1|1){const viewport=this.carouselViewport?.nativeElement;if(!viewport)return;const first=viewport.querySelector<HTMLElement>('.story-carousel-item');const styles=getComputedStyle(viewport);const gap=parseFloat(styles.columnGap||styles.gap)||0;const step=(first?.getBoundingClientRect().width||viewport.clientWidth)+gap;const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;viewport.scrollBy({left:direction*step,behavior:reduce?'auto':'smooth'});window.setTimeout(()=>this.updateCarouselState(),reduce?0:220)}updateCarouselState(){const viewport=this.carouselViewport?.nativeElement;if(!viewport)return;const max=Math.max(0,viewport.scrollWidth-viewport.clientWidth);this.atCarouselStart.set(viewport.scrollLeft<=1);this.atCarouselEnd.set(viewport.scrollLeft>=max-1)}}
